package com.roadmap.backendapi.entity.enums;

/**
 * Enum representing the roles of a user.
 * This enum is used to categorize different types of users in the application.
 *
 * @see com.roadmap.backendapi.entity.User
 */
public enum UserRoles {
    ADMIN,
    USER,
    GUEST,
}
